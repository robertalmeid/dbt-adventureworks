version: 2

models:
  - name: dim_motivos
    description: >
      Tabela de motivos.    
    columns:
    - name: PK_MOTIVO
      description:  chave primária.
      tests:
        - unique
        - not_null