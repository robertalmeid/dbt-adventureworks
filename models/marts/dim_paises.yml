version: 2

models:
  - name: dim_paises
    description: >
      Tabela de paises.    
    columns:
    - name: PK_PAISID
      description:  chave primária.
      tests:
        - unique
        - not_null