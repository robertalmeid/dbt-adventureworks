version: 2

models:
  - name: dim_cidades
    description: >
      Tabela com das cidades, baseada na tabela de endereços.    
    columns:
    - name: PK_ENDERECOID
      description:  chave primária.
      tests:
        - unique
        - not_null