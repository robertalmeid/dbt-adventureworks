version: 2

models:
  - name: dim_produtos
    description: >
      Tabela de produtos.
    columns:
      - name: pk_produtoid
        description: chave primária.
        tests:
          - unique
          - not_null
