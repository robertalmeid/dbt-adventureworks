version: 2

models:
  - name: dim_estados
    description: >
      Tabela com de estados.    
    columns:
    - name: PK_ESTADOID
      description:  chave primária.
      tests:
        - unique
        - not_null