version: 2

models:
  - name: dim_calendario
    description: >
      Tabela com as datas de vendas em formato data, ano, mes, dia.    
    columns:
    - name: pk_data
      description:  chave primária.
      tests:
        - unique
        - not_null