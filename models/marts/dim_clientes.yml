version: 2

models:
  - name: dim_cliente
    description: >
      Tabela com de clientes.    
    columns:
    - name: pk_clienteid
      description:  chave primária.
      tests:
        - unique
        - not_null