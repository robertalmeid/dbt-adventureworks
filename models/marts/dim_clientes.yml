version: 2

models:
  - name: dim_cliente
    description: >
      Tabela com de clientes com join de cidade, estado e pais.    
    columns:
    - name: PK_CLIENTEID
      description:  chave primária.
      tests:
        - unique
        - not_null